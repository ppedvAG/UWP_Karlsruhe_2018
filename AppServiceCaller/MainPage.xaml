<Page
    x:Class="AppServiceCaller.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppServiceCaller"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Viewbox>
        <RelativePanel Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel x:Name="s1" Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Margin="0 0 10 0">Name der Task:</TextBlock>
                <TextBox x:Name="textboxTitle" VerticalAlignment="Center" Width="100" />
                <ListBox x:Name="tbStatus"></ListBox>
            </StackPanel>
            <Button Click="Button_Click" 
                RelativePanel.AlignHorizontalCenterWith="s1" Margin="5" RelativePanel.Below="s1" HorizontalAlignment="Center" Content="Create Task" />
        </RelativePanel>
    </Viewbox>
</Page>
